// Re-export Convex generated API for client usage.
// The generated file only exports references (no server code),
// so it is safe to import in the Next.js client bundle.
export { api } from "@repo/convex/api";
